{
  "client": {
    // full client request jsonObject
  },
  "response": {
    "sse": {
      // if stream=true, output jsonobject one by one 
      "chunk_1": {},
      "chunk_2": {}
    },
    "full": {
      // output jsonobject, full response in non-streaming format. convert sse to non-streaming format if needed.
    }
  }
}